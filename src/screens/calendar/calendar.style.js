import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles = {

    container: {
        backgroundColor: COLORS.white,
        flex: 1,
        padding: 12
    },

    text: {
        fontSize: FONT_SIZE.md,
        color: COLORS.cinza1,
        marginBottom: 15,
        marginLeft: 12
    }

}